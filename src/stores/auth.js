export const userAuthStore = defineStore('auth', {
    state: () => ({}),

    getters: {},

    actions: {
        async login(credentials) {},

        async register(credentials) {},

        async logout() {},
    },
})
